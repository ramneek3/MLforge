apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: mlforge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_USER
              value: mlforge
            - name: POSTGRES_DB
              value: mlforge
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mlforge-secrets
                  key: POSTGRES_PASSWORD
          ports:
            - containerPort: 5432
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "500m"
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "mlforge"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "mlforge"]
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: mlforge
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
